const bubbleSort = arr => {
    let numSwaps = 0;
    for(let i = 0; i < arr.length; i++){
        if(arr[i] > arr[i + 1]{
            console.log('inequality');
        }
    }
}

bubbleSort([3, 2, 1]);